---
name: Scheduled site build
on:
  schedule:
  - cron: '0 3,4,5,6,7,8 * * 0,1,5,6'
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Trigger build
      env:
        TOKEN: ${{ secrets.HOOK }}
      run: >-
          curl -X POST ${HOOK}